<h1>Listing microposts</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @microposts.each do |microposts| %>
      <tr>
        <td><%= microposts.content %></td>
        <td><%= microposts.user_id %></td>
        <td><%= link_to 'Show', microposts %></td>
        <td><%= link_to 'Destroy', microposts, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Micropost', new_micropost_path %>
